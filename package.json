{
  "name": "hortacloud",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "postbuild": "cpy '**/*' '!**/*.ts' '../build/' --cwd=src --parents",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "clean": "del build cdk.out",
    "cleancdk": "npm run clean && npm run build && npm run cdk"
  },
  "peerDependencies": {
    "aws-cdk-lib": "^2.4.0",
    "constructs": "~10.0.13"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.4.0",
    "constructs": "^10.0.13",
    "crypto": "^1.0.1",
    "cpy-cli": "^3.1.1",
    "del-cli": "^4.0.1",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  }
}
